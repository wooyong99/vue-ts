export interface UserListResponse {
    id : number
    email : string,
    password : string,
    username : string,
    createdAt : string
}

export interface UserDetailResponse {
    id: number
    email: string
    password: string
    username: string
    createdAt: string
}